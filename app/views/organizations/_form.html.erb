<%= form_with(model: organization, local: true) do |form| %>
  <% if organization.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(organization.errors.count, "error") %> prohibited this organization from being saved:</h2>

      <ul>
      <% organization.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name, 'Organization Name' %>
    <%= form.text_field :name, class: "input", placeholder: "Name of organization" %>
  </div>

  <div class="field">
    <%= form.label :desc, "Description of Organization" %>
    <%= form.text_area :desc, rows: "7", class: "textarea", placeholder: "Give a short description of the organization" %>
  </div>

  <div class="actions">
    <%= form.submit "Submit", class: "button is-primary" %>
  </div>
<% end %>
